#!/usr/bin/env bash
set -euo pipefail

mapfile -d '' files < <(git diff --cached --name-only --diff-filter=ACMR -z)

if [ "${#files[@]}" -eq 0 ]; then
  exit 0
fi

npx --yes prettier@3 --write --ignore-unknown -- "${files[@]}"
git add -- "${files[@]}"
